/**
 * @format
 * @flow strict-local
 */

import React, {
  useState, useEffect,
} from 'react';
import {
  View, StyleSheet,
} from 'react-native';

const UIKitTestScreen = () => {
  return (<></>)
};

const styles = StyleSheet.create({
});

export default UIKitTestScreen;
